name: wayapay
description: A Dart/Flutter client for the WayaPay payment collection and payout API.
version: 0.1.0
homepage: https://wayapay.ng

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter: